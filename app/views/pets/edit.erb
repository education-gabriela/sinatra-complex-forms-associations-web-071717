<h1>Update Pet</h1>
<form action="/pets/<%= @pet.id %>" method="post">
  <input type="hidden" id="hidden" value="patch" name="_method">
  Name: <br><input type="text" name="pet[name]" id="pet_name" value="<%= @pet.name %>">
  <br><br>
  Owner:<br>
  <% @owners.each do |owner| %>
      <%  @pet.owner.id == owner.id ? checked = "checked" : checked = "" %>
      <input value="<%= owner.id %>" type="radio" name="pet[owner_id]" <%=checked%> id="<%= owner.id %>"><%= owner.name %><br>
  <% end %>

  Or create a new Owner: <br>
  <input name="owner[name]" type="text" id="owner_name"><br><br>
  <input type="submit" value="Update Pet" id="submit">
</form>